import theme, { COLORS, SIZES, FONTS, darkTheme, lightTheme } from './theme';
import dummyData from './dummyData';
import constants from './constants';
import icons from './icons';

export {
    COLORS,
    SIZES,
    FONTS,
    darkTheme,
    lightTheme,
    dummyData,
    constants,
    icons,
}